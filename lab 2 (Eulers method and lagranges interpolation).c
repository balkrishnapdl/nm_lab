#include<stdio.h>
#include<math.h>
int main()
{
    int i,n;
    float h,x0,y0,yn,x,y,a,b,c,d;
    printf("Enter the initial condition(x0,y0):");
    scanf("%f,%f",&x0,&y0);
    printf("Enter the step size:");
    scanf("%f",&h);
    
}